---
const webhook = import.meta.env.WEBHOOK;

const res = await fetch(webhook, {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  },
  body: await Astro.request.blob()
});

return new Response(await res.text(), {
  status: res.status == 204 ? 200 : res.status
});
---
