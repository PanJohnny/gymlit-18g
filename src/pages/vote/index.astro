---
import Layout from "../../layouts/Layout.astro";

// check if success or error in query params
const success = Astro.url.searchParams.has("success");
const error = Astro.url.searchParams.has("error");
const closed = Astro.url.searchParams.has("closed");

let raw = Astro.url.searchParams.get("raw");
---
<Layout title="Hlasování" description="Velké hlasování o nejlepší hlášku">
    <div class="body">
        {success &&
                <div class="success-message">Hlas odevzdán!</div>}
        {closed &&
                <div class="error-message">Hlasování uzavřeno. Pokud vidíte tuto zprávu po odeslání hlasu, tak hlas
                    nebyl započítán.
                </div>}
        {error &&
                <div class="error-message">Něco se pokazilo. Napište prosím Honzovi a pošlete mu následující text:
                    <pre>{raw}</pre>
                </div>}
        <p>Ve dvoukolovém hlasování budeme vybírat nejlepší zapsanou hlášku za posledních osm let. V prvním kole budeme
            přijímat nominace, ve druhém už půjde o všechno.</p>
        <p>Každý hlasuje pouze jednou a jeho hlas je vázaný na jeho jméno. Odevzdané hlasy budeme kontrolovat.</p>
        <h2>První kolo - nominace</h2>
        <strong>Postupující hlášky:</strong>
        <ul>
            <li>
                „Odstup Satane od tabule!“ Řekla zvýšeným hlasem paní profesorka Rozumová Filipovi, který se dopustil na
                tabuli mnoha matematických zločinů. 6
            </li>
            <li>
                „Mohli bychom nocovat ve škole, ALE BEZ ALKOHOLU. Říkejte tomu předsudky nebo drby, já tomu říkám
                fakta.“ 5
            </li>
            <li>
                „Před chvílí jsem se ohnula a už nenarovnala, takže teď tady chodím jak Kvasimódo.“ 5
            </li>
            <li>
                „Jestli sis opravdu četl biologii pod tou lavicí, tak tu tužku sežeru, to uvidíš,“ oznámil pan profesor
                Lorenc Václavovi. 4
            </li>
            <li>
                „Pořád se tady napájíš a ještě u toho mlaskáš“ Ososčil pan profesor Tobiase. 4
            </li>
            <li>
                „Kdo z vás neví, kde jsou Pardubice?“. Nikdo nezvedl ruku. „Pardubice jsou přeci na Moravě.“ 4
            </li>
            <li>
                „Ty zločinče, kam vede ta kolmice, všichni to vědí, že jsi zločinec.“ 4
            </li>
            <li>
                „Ale já vás nechtěl. Já chtěl septimy a oktávy.“ 4
            </li>
            <li>
                „Svět se zbláznil a já proti tomu bojuju!“ 4
            </li>
        </ul>
        <h2>Druhé kolo - sestavení žebříčku hlášek</h2>
        <p>V tomto kole budete přidělovat body jednotlivým hláškám, ty pak budou seřazeny podle celkového počtu
            bodů.</p>
        <div class="countdown-overlay">
            <a href="/vote/rate" class="button large">
                Hlasovat v druhém kole
            </a>
            <div class="countdown" data-time="2025-12-22T08:00:00Z">
                <div>
                    <span class="number days">00</span>
                    <span class="label">dní</span>
                </div>
                <div>
                    <span class="number hours" id="hours">00</span>
                    <span class="label">hodin</span>
                </div>
                <div>
                    <span class="number minutes" id="minutes">00</span>
                    <span class="label">minut</span>
                </div>
                <div>
                    <span class="number seconds" id="seconds">00</span>
                    <span class="label">sekund</span>
                </div>
            </div>
        </div>
        <p>Hlasování bude otevřeno v pondělí 22.12. v 9:00 a uzavřeno v úterý 23.12. ve 12:00.</p>
        <h2>Výsledky</h2>
        <p>Výsledky budou po zpracování vyhlášeny na této stránce a na WhatsAppu.</p>
        <h2>Řešení problémů</h2>
        <p>Máte problém? Neváhejte co nejdřív napsat.</p>
    </div>
</Layout>

<style>
    .countdown-overlay {
        height: 50px;
        position: relative;
    }

    .countdown {
        display: flex;
        gap: 15px;
        position: absolute;
        background-color: var(--light);
        border: solid black 1px;
        border-radius: 3px;
        padding: 3px;
        top: 0;
    }

    .countdown div {
        text-align: center;
    }

    .number {
        font-size: 2em;
        font-weight: bold;
    }

    .label {
        font-size: 0.9em;
        color: #666;
    }

    .button {
        background-color: dodgerblue;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        font-weight: bold;
        display: inline-block;
        text-align: center;
        margin-bottom: 10px;
    }

    .large {
        font-size: 1.3em;
        padding: 10px 20px;
    }

    .success-message {
        background-color: #d4edda;
        color: #155724;
        padding: 10px;
        border-radius: 5px;
        margin-bottom: 15px;
        border: 1px solid #c3e6cb;
    }

    .error-message {
        background-color: #f8d7da;
        color: #721c24;
        padding: 10px;
        border-radius: 5px;
        margin-bottom: 15px;
        border: 1px solid #f5c6cb;
    }
</style>

<script>
    document.querySelectorAll('.countdown').forEach(countdownElement => {
        const targetTime = new Date(countdownElement.getAttribute('data-time')).getTime();

        const correctLabels = {
            days: ['den', 'dny', 'dní'],
            hours: ['hodina', 'hodiny', 'hodin'],
            minutes: ['minuta', 'minuty', 'minut'],
            seconds: ['sekunda', 'sekundy', 'sekund']
        };

        function getCorrectLabel(number, labels) {
            if (number === 1) {
                return labels[0];
            } else if (number >= 2 && number <= 4) {
                return labels[1];
            } else {
                return labels[2];
            }
        }

        let interval = setInterval(updateCountdown, 1000);

        function updateCountdown() {
            const now = new Date().getTime();
            const distance = targetTime - now;

            if (distance < 0) {
                countdownElement.remove();
                clearInterval(interval);
                return;
            }

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            countdownElement.querySelector('.days').innerText = String(days).padStart(2, '0');
            countdownElement.querySelector('.hours').innerText = String(hours).padStart(2, '0');
            countdownElement.querySelector('.minutes').innerText = String(minutes).padStart(2, '0');
            countdownElement.querySelector('.seconds').innerText = String(seconds).padStart(2, '0');

            // update the labels
            countdownElement.querySelector('.days').nextElementSibling.innerText = getCorrectLabel(days, correctLabels.days);
            countdownElement.querySelector('.hours').nextElementSibling.innerText = getCorrectLabel(hours, correctLabels.hours);
            countdownElement.querySelector('.minutes').nextElementSibling.innerText = getCorrectLabel(minutes, correctLabels.minutes);
            countdownElement.querySelector('.seconds').nextElementSibling.innerText = getCorrectLabel(seconds, correctLabels.seconds);
        }

        updateCountdown();
    });
</script>