---
import Layout from "../layouts/Layout.astro";
import Card from "../components/Card.astro";
---

<Layout title="Kronika třídy 5. G">
	<script is:inline>
		const old = localStorage.getItem("old");
		if (old == "true") {
			location.href = "/old";
		} else {
			location.href = "/kronika";
		}
	</script>
</Layout>